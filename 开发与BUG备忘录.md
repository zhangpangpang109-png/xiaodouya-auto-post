# 新榜小豆芽全自动发布脚本 - 开发与BUG修复备忘录

这份文档记录了我们在开发和调试 `xiaodouya_poster.py` 期间经历的核心逻辑迭代和踩过的坑，用于为以后（甚至AI“失忆”后）维护和升级脚本提供上下文。

## 一、 核心业务逻辑

1. **业务目标**：从指定的本地文件夹（如 `自动发布`）读取所有短视频，依次控制“新榜小豆芽”客户端，按账号（`01号` 到 `10号` 轮转）自动发布，发完后将文件移入 `已发布` 文件夹。
2. **账号切换机制**：通过点击左侧导航栏的账号列表（提取 `Text` 类型控件），实现账号的切换。
3. **导航与上传链路**：
   - 账号主页 -> 点击左侧“变现中心”展开二级菜单 -> 点击“我的任务”
   - 在任务列表找到对应任务（如“三国冰河时代矩阵短视频”） -> 点击“查看详情”
   - 在详情页点击红色的“上传视频” -> 弹出 Windows 文件选择对话框
   - 通过剪贴板或键盘输入文件绝对路径 -> 敲击 `Enter` 键确认上传
4. **填写信息与封面**：
   - **标题**：读取文件名（不含扩展名），复制并粘贴到“填写作品标题”的编辑框。
   - **话题**：点击独立的“#添加话题”文字按钮，激活话题输入框后，逐个粘贴话题，并在每个话题后发送 `{ENTER}` 使其变为蓝色标签。
   - **封面**：找到 `Image` 类型的“智能推荐封面”区域，点击第一张图，再点击弹窗的“确定”或“应用”。
5. **发布与清理**：
   - 滑动页面到底部 -> 点击最终的“发布”按钮
   - 发布完成后，将原视频通过 `shutil.move` 移至 `已发布` 目录。

---

## 二、 踩坑记录与解决方案 (BUG 进化史)

### 1. 悬浮资料卡遮挡 / 账号误触
- **现象**：在左侧列表点击账号名字（如“02号疯少...”）时，小豆芽第一下点击往往会弹出一个包含该账号详细信息的悬浮卡片，遮挡了后续操作。
- **错误尝试**：尝试点击屏幕下方 `(200, 800)` 的绝对坐标消除弹窗，结果导致误触了排在下面的其他账号（如点击 02 号时点到了 05 号）。
- **最终解决**：采用**相对坐标安全点击法**。先在左侧菜单栏寻找固定的“任务市场”文本按钮，获取它的底部坐标，然后在“任务市场”正下方 30 像素（约1厘米处）的空白区域点击鼠标左键。这个区域永远是安全的。

### 2. 找不到“我的任务” (全屏状态丢失与菜单折叠)
- **现象 1**：脚本有时会报错找不到“我的任务”。原因是 `win32gui.SetForegroundWindow` 在某些时候会使小豆芽退出最大化状态，导致侧边栏变形或隐藏。
- **解决 1**：所有激活窗口的操作均统一通过 `win32gui.ShowWindow(hwnd, win32con.SW_MAXIMIZE)` 强制保持全屏。
- **现象 2**：即使全屏，有时也找不到“我的任务”。
- **解决 2**：发现“我的任务”是二级菜单！必须先点击一级菜单“变现中心”将其展开。并在代码中加入了点击“变现中心”文字偏左侧位置（避开右侧折叠箭头）的逻辑。

### 3. 详情页白屏卡死 / 页面迷失
- **现象**：部分账号点击“查看详情”后，页面卡在重定向的白屏，或者停留在之前未完成的发布页面。
- **错误尝试**：尝试发送快捷键 `F5` 刷新，结果小豆芽有时会新开一个不带左侧导航栏的独立页面。
- **最终解决**：**暴力重置法**。如果在当前页面找不到想要的入口，脚本会扫描页面上所有的“关闭”按钮（即标签页关闭按钮），把当前账号的所有标签页全关掉。然后重新从左侧列表点击该账号进入，确保从最干净的首页重新开始。

### 4. “上传视频”按钮找不到 (控件坐标过滤误伤)
- **现象**：在任务详情页找不到“上传视频”按钮。
- **原因**：之前为了在“创作者主页”点击顶部的上传按钮，加了一个 `min_left=1200` 的横坐标限制。但在任务详情页中，上传按钮在页面偏左下的位置，被这个条件过滤掉了。
- **解决**：彻底删除 `min_left` 限制条件。

### 5. Windows 文件选择对话框的玄学
- **现象**：填入视频绝对路径后，点击“打开(O)”按钮没有反应。
- **原因**：可能由于焦点丢失或 Win32 与 UIA 的后端交互差异，鼠标 `click()` 没有触发系统的确认事件。
- **解决**：填入路径后，直接使用 `keyboard.send_keys("{ENTER}")` 敲击回车键，完美触发文件上传。

### 6. 弹窗挡住封面 / 话题加不进标题
- **现象 1**：点击智能推荐封面后，弹出的“确定”按钮找不到，因为页面上有“智能封面已上线”等新手引导弹窗。
- **解决 1**：增加 `clear_onboarding_popups` 函数，在操作前无脑扫描并点击所有名为“我知道了”、“关闭”、“确定”的干扰按钮。
- **现象 2**：标题行字数超限，导致拼接在后面的 `#话题` 被截断，没有生效。
- **解决 2**：**拆分填写**。标题专门填入“填写作品标题”的 `Edit` 框。话题专门去寻找并点击“#添加话题”文字按钮，在弹出的独立输入框内通过回车逐个激活话题标签。

### 7. 文件移动失败 (PermissionError 占用冲突)
- **现象**：发布完成后，把视频移入“已发布”文件夹时报 `[Errno 13] Permission denied`。
- **原因**：视频刚传完，小豆芽后台进程或系统缓存尚未释放文件句柄。
- **解决**：增加重试与强杀机制。遇到占用则等待 5 秒，最多重试 10 次。如果 Python 的 `shutil.move` 依然失败，则调用系统底层的 `os.system('move /Y ...')` 强制移动。

---

### 8. 话题词被填成视频标题（剪贴板竞态）
- **现象**：配置的 3 个话题词中，前 1-2 个话题被填成了视频标题（文件名），只有最后一个话题正确。
- **根因**：`copy_text()` 通过 win32 剪贴板写入话题词后，`CloseClipboard` 发出剪贴板变更通知，但小豆芽进程收到通知并读取内容需要时间。原代码 `sleep(0.15)` 不够，前两个话题 `^v` 粘贴时小豆芽还没读到新剪贴板内容，读到的是上一次粘贴的视频标题。标题粘贴不出问题是因为前面有 `^a{BACKSPACE}` + `sleep(0.2)` 自然间隔。
- **解决**：`copy_text(tag)` 后 `sleep` 从 0.15s 增大到 **0.5s**，给 Windows 剪贴板变更通知链充足的传播时间。涉及文件 `xiaodouya_poster.py` L1079。

### 9. 小红书话题词硬编码，改配置不生效
- **现象**：把 `xhs_xiaodouya_config.json` 的 `description_text` 从 `#全民天天麻将` 改成 `#富豪麻将`，执行脚本后话题仍是旧的。
- **根因**：`xhs_xiaodouya_poster.py` 的 `fill_title_and_description()` 方法（L901、L905）把话题词写死了 `"#全民天天麻将小游戏"` + `"@全民天天麻将小游戏"`，完全没有读取 `self.config.description_text`。
- **解决**：删除硬编码，改为 `self.paste_text(self.config.description_text)`。同时移除了 `@` 账号逐字敲入逻辑。以后改话题只需编辑 `xhs_xiaodouya_config.json` 的 `description_text` 字段。

---

## 三、 项目配置与AI协作规范

### 3.1 配置说明
- **Python 版本**: 3.x
- **核心依赖库**: `pywinauto==0.6.9`, `pywin32==311` (见 `requirements.txt`)
- **抖音配置**: `xiaodouya_config.json`（账号列表、视频路径、话题词等）
- **小红书配置**: `xhs_xiaodouya_config.json`（账号列表、视频路径、`description_text` 话题词等）
- **启动方式**：
  - 抖音：`启动抖音自动发布.bat` → 调用 `xiaodouya_launcher.py` → `xiaodouya_poster.py`
  - 小红书：`启动小红书自动发布.bat` → 调用 `xhs_xiaodouya_launcher.py` → `xhs_xiaodouya_poster.py`

### 3.2 CLAUDE.md
项目根目录 `CLAUDE.md` 包含 12 条 AI 辅助开发规则（先思后码、简单至上、外科手术式修改等），所有修改本项目的行为都应遵守此规则。两个平台脚本**互不共用代码**，修改一个不得顺带改动另一个。
